const SUCCESS_RESPONSE = { success: true };

const STANDARD_FRACTION = 10000;

const STANDARD_PRECISION = 4;

const ZERO_AMOUNT = {
  currency: "INR",
  cent_amount: 0,
  fraction: STANDARD_FRACTION
};

module.exports = { SUCCESS_RESPONSE, STANDARD_PRECISION, ZERO_AMOUNT, STANDARD_FRACTION };
